<div class="sidebar">

<div class="col-xs-4">
  <ul class="nav nav-pills nav-stacked">
    <li></li>
  </ul>

<div class="side_user">


  <table>
    <tr>
      <tb><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %>
        <!-- rails g migration AddProfileImageIdToUsers profile_image_id:string を実行しないと表示できない -->
        <% if @user.id == current_user.id %>
      <br>
      
      <% end %>
      </tb><br>
    </tr>
    <tr>
      <th>neme</th>
      <th><%= user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= user.introduction %></th>
    </tr>
    <tr>
      <th>
        <%= link_to edit_user_path(@user), class: 'btn btn-default' do %>
        <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
        <% end %>
      </th>
    </tr>
  </table>
</div>

<div class="new">
 <h3 class="new-title">New book</h3>
  <%= form_for(book) do |f| %>
  


  <p>Title</p>
    <%= f.text_field :title %>
 
    <p>Opinion</p>
    <%= f.text_area :body %><br>
 
    <%= f.submit 'Create Book' %>
 
  <% end %>
</div>

</div>

</div>

